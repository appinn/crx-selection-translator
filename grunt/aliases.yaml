dev:
  - clean:dev
  - uglify:lib-dev
  - copy:common-dev
  - copy:manifest-dev
  - copy:images-dev
  - copy:js-dev
  - handlebars
  - less
  - jade
  - watch

release:
  - clean:dist

default:
  - dev
